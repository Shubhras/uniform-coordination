import SignInClient from './_components/SignInClient'

/**
 * Sign In Page component.
 * Renders the SignInClient component for user authentication.
 *
 * @returns {JSX.Element} Sign-in page view.
 */
const Page = () => {
    return <SignInClient />
}

export default Page
