import SignInClient from './_components/SignInClient'

/**
 * Sign In Page component.
 * Renders the SignInClient component for user sign-in.
 *
 * @returns {JSX.Element} Sign-in page container.
 */
const Page = () => {
    return <SignInClient />
}

export default Page
